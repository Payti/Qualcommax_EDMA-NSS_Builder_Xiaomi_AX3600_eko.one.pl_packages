#
# EDMA-NSS variant fragment for the Xiaomi AX3600.
# Appended to devices/xiaomi_ax3600/config by prepare-build.sh.
#
# NSS hardware offload on the upstream qca-edma/qca-ppe ethernet drivers:
# tree JuliusBairaktaris/openwrt-nss-edma (nss-edma-rework) + feed
# JuliusBairaktaris/nss-packages (edma-nss). Docs:
# https://github.com/JuliusBairaktaris/openwrt-nss-edma/wiki
#

## NSS data plane + connection offload (ECM, PPPoE).
## kmod-qca-ppe-nss (the data-plane glue) is pulled in by kmod-qca-nss-drv.
CONFIG_PACKAGE_kmod-qca-nss-drv=y
CONFIG_PACKAGE_kmod-qca-nss-ecm=y
CONFIG_PACKAGE_kmod-qca-nss-drv-pppoe=y
## kmod-qca-nss-drv-bridge-mgr: hardware offload of wired LAN-bridge forwarding
## (brought up by nss-up alongside the rest of the plane).
CONFIG_PACKAGE_kmod-qca-nss-drv-bridge-mgr=y
## kmod-qca-mcs: IGMP/MLD bridge snooping; enables the ECM multicast classifier
## (ECM_MULTICAST_ENABLE) so snooped groups replicate in PPE hardware. Loaded by
## nss-up before ECM. It DEPENDS on the mroute kernel options, which it
## auto-selects (kept explicit below as belt-and-suspenders).
CONFIG_PACKAGE_kmod-qca-mcs=y
CONFIG_KERNEL_IP_MROUTE=y
CONFIG_KERNEL_IPV6_MROUTE=y
## Routed (L3) multicast offload (IPTV WAN->LAN). igmpproxy proxies LAN IGMP
## joins upstream and installs kernel ipmr MFC (S,G) entries; ECM's routed-
## multicast classifier (ECM_MULTICAST_ENABLE, already on) turns each MFC entry
## into a PPE hardware rule. Pure userspace (no kmod, no extra kernel config).
## Started opt-in by /usr/sbin/nss-up; its init is disabled at boot by
## uci-defaults 997. INERT unless a WAN multicast source exists (no IPTV here ->
## no-op in production; proven by the routed-mc gate).
CONFIG_PACKAGE_igmpproxy=y
CONFIG_NSS_FIRMWARE_VERSION_12_5=y
# AX3600 = 512MB RAM: NSS MEDIUM memory profile (HIGH is the ipq807x default and wrong here)
CONFIG_NSS_MEM_PROFILE_MEDIUM=y

## ath11k NSS Wi-Fi offload (wifili). Boots host-mode (nss_offload=0,
## frame_mode=2); /usr/sbin/nss-up moves the radios onto the NSS data path.
CONFIG_ATH11K_NSS_SUPPORT=y
CONFIG_PACKAGE_MAC80211_NSS_SUPPORT=y
CONFIG_ATH11K_MEM_PROFILE_512M=y

## NSS qdisc/SQM (nss-edma.qos) + the SQM LuCI app
CONFIG_PACKAGE_kmod-qca-nss-drv-qdisc=y
CONFIG_PACKAGE_kmod-qca-nss-drv-igs=y
CONFIG_PACKAGE_sqm-scripts-nss=y
CONFIG_PACKAGE_luci-app-sqm=y
